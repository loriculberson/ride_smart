<p> <%= bike_event.user.username %> reports: <br>
<%= bike_event.event_kind %> on 
<%= bike_event.date %> @
<%= bike_event.time %></p>
<p> Details: <%= bike_event.details %> </p>


<div class="remove-marker">
  <%=link_to "Remove marker", bike_event_path(bike_event.id), remote: true, method: :delete, class: "remove-marker"%>
</div>

<!-- <div class="remove-marker">
  <%#link_to "Remove marker", bike_event_path(bike_event.id) , class: "remove-marker"%>

  Must set up a click listener on the document. Then must write an ajax request, that inclues the type: delete and the url
</div> -->

